# SDK Review Definitions

This directory contains review surfaces for a future external SDK.

The sandbox repository should remain the internal experimental workspace.
A downstream SDK should expose curated download surfaces such as:
- selected reports
- aggregate datasets
- manifests
- reproducibility bundles
