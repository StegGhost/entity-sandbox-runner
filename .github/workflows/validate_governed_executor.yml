name: Validate Governed Executor

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - "authority_resolver.py"
      - "predictive_engine.py"
      - "decision_state_recorder.py"
      - "receipt_chain.py"
      - "receipt_chain_verifier.py"
      - "governed_executor.py"
      - "u_signal_monitor.py"
      - "stability_governor.py"
      - "test_governed_executor.py"
      - "test_tamper_block.py"
      - ".github/workflows/validate_governed_executor.yml"

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Show Python version
        run: python --version

      - name: Run governed executor test
        run: python test_governed_executor.py

      - name: Run tamper block test
        run: python test_tamper_block.py

      - name: Run state reconstruction test
        run: python test_state_reconstruction.py
