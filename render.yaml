services:
  - type: web
    name: steggate-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn steggate_api:app --host 0.0.0.0 --port 10000
    autoDeploy: true
