services:
  - type: web
    name: steggate-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn steggate_api:app --host 0.0.0.0 --port $PORT
